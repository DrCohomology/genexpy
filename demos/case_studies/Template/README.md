This directory contains the template to run a reliability analysis on experimental results. 
To repeat the analysis, copy-paste the directory, edit the configuration file as detiled in `reliability_analysis.ipynb`, and run the notebook.

The configuration file is initialized for the analysis of the categorical encoders experimental data.